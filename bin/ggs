#!/bin/bash
for dir in $(ls -1 --color=no $PROJECTPATH); do
  project="$PROJECTPATH/$dir"
  if [[ -d "$project/.git" ]]; then
    (
    cd $project
    if [[ $(git status -uno -s | wc -l) -ne 0 ]]; then
      echo $dir
    fi
    )
  fi
done
